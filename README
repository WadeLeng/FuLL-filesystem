================================================================
                   FuLL filesystem Version 0.1
================================================================
    FuLL(Fuse Libevent Leveldb)是基于leveldb的用户态文件系统。
web端使用libevent接口，提供高并发。本机使用FUSE接口，只读文件系
统，方便查看。

    系统web端提供put, get, delete方式来进行增、取、删操作。
server端使用方式，详见-h 选项。客户端假如插入一条key = foo
value = bar的数据。使用http://IP:Port/?opt=put&key=foo&value=bar
完成。value长度较大可以使用POST报文传递，正文是value值。
    
    使用前，系统需要安装FUSE, Libevent, Leveldb。

    Contents of the dirctory tree is below.
    ./		- sources
    ./doc	- documents
	./test	- test program


    e-mail: wade.hit@gmail.com

== END OF FILE ==
